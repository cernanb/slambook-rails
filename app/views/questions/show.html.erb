<h1><%= @book.title %></h1>
<%= @question.content %> <br/>
<%= form_for ([@book, @question, Answer.new]) do |f| %>
  <%= f.text_field :content %>
  <% if @book.questions.last == @question %>
    <%= f.submit "Complete" %>
  <% else %>
  <%= f.submit "Next Question" %>
  <% end %>
<% end %>
